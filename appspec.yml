version: 0.0
os: linux
files:
  - source: /
    destination: /home/ubuntu/my-app  # हा पाथ वापरा
hooks:
  ApplicationStop:
    - location: scripts/stop_server.sh
      timeout: 300
      runas: ubuntu
  AfterInstall:
    - location: deploy.sh
      timeout: 300
      runas: root
